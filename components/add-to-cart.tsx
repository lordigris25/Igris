'use client';import {useState}from'react';import {useCart}from'./cart-provider';
export function AddToCart({slug,buyNow=false}:{slug:string;buyNow?:boolean}){const{add}=useCart();const[q,setQ]=useState(1);return <div className="purchase"><div className="quantity"><button onClick={()=>setQ(Math.max(1,q-1))} aria-label="Decrease quantity">−</button><span>{q}</span><button onClick={()=>setQ(q+1)} aria-label="Increase quantity">+</button></div><button className="button gold" onClick={()=>{add(slug,q); if(buyNow)location.href='/checkout'}}>{buyNow?'Buy now':'Add to bag'} <b>↗</b></button></div>}
