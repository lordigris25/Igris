import Link from 'next/link';import {Product,formatPrice}from '@/lib/products';
export function ProductVisual({product,large=false}:{product:Product;large?:boolean}){return <div className={`product-visual ${large?'large':''} ${product.slug}`} aria-label={`${product.name} branded media placeholder`}><span className="visual-number">0{product.id==='pitti'?1:product.id==='bajot'?2:product.id==='mala'?3:product.id==='topi'?4:5}</span><div className="ornament"/><strong>{product.name}</strong><small>Product media placeholder<br/>Replace in /public/products/{product.slug}/</small></div>}
export function ProductCard({product,index=0}:{product:Product;index?:number}){return <article className="product-card" style={{'--n':index} as React.CSSProperties}><Link href={`/product/${product.slug}`}><ProductVisual product={product}/><div className="product-meta"><div><span>{product.category}</span><h3>{product.name}</h3></div><div className="price">{formatPrice(product.price)}{product.priceNote&&<small> / pair of 2</small>}</div></div><span className="view">View product <b>↗</b></span></Link></article>}
