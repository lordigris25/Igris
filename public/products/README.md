# Product media slots

Each product directory accepts `hero.jpg`, gallery images, `thumbnail.jpg`, and optionally `video.mp4`. The current storefront deliberately renders branded CSS media placeholders because approved product photography has not been supplied. Update the product data in `lib/products.ts` to point at approved assets without changing page layouts.
