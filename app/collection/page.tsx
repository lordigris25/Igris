import {products}from '@/lib/products';import {ProductCard}from '@/components/product-card';export const metadata={title:'Collection'};export default function Collection(){return <main className="page collection-page"><header className="page-head"><p className="eyebrow">THE INITIAL EDIT</p><h1>The <em>collection.</em></h1><p>Five essentials. One thoughtfully considered beginning.</p></header><div className="collection-grid">{products.map((p,i)=><ProductCard key={p.id} product={p} index={i}/>)}</div></main>}
