import {notFound}from'next/navigation';import Link from'next/link';import{getProduct,products,formatPrice}from'@/lib/products';import{ProductCard,ProductVisual}from'@/components/product-card';import{AddToCart}from'@/components/add-to-cart';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const p=getProduct((await params).slug);if(!p)return notFound();const related=products.filter(x=>x.id!==p.id).slice(0,3);const schema={'@context':'https://schema.org','@type':'Product',name:p.name,description:p.description,offers:{'@type':'Offer',price:p.price,priceCurrency:p.currency,availability:'https://schema.org/InStock'}};return <main className="product-page"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><Link className="back" href="/collection">← Collection</Link><section className="product-hero"><ProductVisual product={p} large/><div className="product-info"><p className="eyebrow">{p.category}</p><h1>{p.name}</h1><p className="product-price">{formatPrice(p.price)} {p.priceNote&&<small>— {p.priceNote}</small>}</p><p className="description">{p.description}</p><dl><div><dt>Available colours</dt><dd>{p.availableColours.length?p.availableColours.join(', '):'Colour details will be shared when available.'}</dd></div><div><dt>Customisation</dt><dd>{p.customisable?'Available for this piece.':'Customisation currently unavailable.'}</dd></div></dl><AddToCart slug={p.slug}/><AddToCart slug={p.slug} buyNow/><a className="ask" target="_blank" href={`https://wa.me/919840400028?text=${encodeURIComponent(`Hi Sri Mishri Decors, I'm interested in the ${p.name}. Could you please share more details?`)}`}>Ask about this product ↗</a></div></section><section className="details"><p className="eyebrow">PRODUCT DETAILS</p><h2>An essential for<br/><em>the ritual.</em></h2><p>Media for this product is currently a branded placeholder. Replace the media files in <code>/public/products/{p.slug}/</code> when photography is ready; the product layout will remain unchanged.</p></section><section className="related"><div className="section-row"><h2>You may also <em>like.</em></h2><Link href="/collection" className="text-link">All products ↗</Link></div><div className="related-grid">{related.map((x,i)=><ProductCard product={x} index={i} key={x.id}/>)}</div></section></main>}
